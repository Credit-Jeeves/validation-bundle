imports:
    - { resource: config_dev.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/cj/routing_test.yml" }
    translator: { fallback: 'test' }
    default_locale: 'test'
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        enabled: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    transport: swiftmailer.transport.file_transport

doctrine:
    dbal:
        dbname:   '%database_name%_test'

mink:
    goutte: ~
#    sahi: ~
#    zombie: ~
#    selenium: ~
    selenium2: ~

