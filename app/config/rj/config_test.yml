imports:
    - { resource: config_dev.yml }

parameters:
    import_1_0_rows_on_the_page: 9
    router.request_context.base_url: '/rj_test.php'
    doctrine.dbal.connection.wrapper_class: 'CreditJeeves\TestBundle\Doctrine\DBAL\PersistedConnection'

    payment_processor.hps.report_path: 'src/RentJeeves/CheckoutBundle/Tests/Data/PaymentProcessor/Heartland/ReportLoader'

    payment_one_time_until_value: '00:01'
    payment_debit_one_time_until_value: '00:01'

    allow_score_track: true

    credit_summary_vendor: experian # supported values are 'transunion' or 'experian'
    scanning_cutoff_time: '23:59' # to avoid failed tests for scanning tab whe tests run after cutoff time

framework:
    router:   { resource: "%kernel.root_dir%/config/rj/routing_test.yml" }
    translator: { fallback: 'test' }
    default_locale: 'test'
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        enabled: true
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    transport: file

doctrine:
    dbal:
        dbname:   '%database_name%_test'

twig:
    strict_variables: true

mink:
    goutte: ~
#    sahi: ~
#    zombie: ~
#    selenium: ~
    selenium2: ~

backup_restore: ~
