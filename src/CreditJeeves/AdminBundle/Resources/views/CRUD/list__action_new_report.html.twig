
{% if admin.isGranted('EDIT', object) %}
    <a href="{{ path('admin_user_new_report', { "id": object.id }) }}" class="btn edit_link getNewReport" title="{{ 'action_report'|trans({}) }}">
        <i class="icon-report"></i>
        {{ 'get.new.report'|trans({}) }}
    </a>
{% endif %}
