{% if admin.isGranted('EDIT', object) %}
    <a href="{{ path('admin_cj_report_list', { "user_id": object.id }) }}" class="btn btn-small" title="{{ 'action_report'|trans({}) }}">
        <i class="icon-report"></i>
        {{ 'action_report'|trans({}) }}
    </a>
{% endif %}
