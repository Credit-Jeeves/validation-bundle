{% if admin.isGranted('EDIT', object)%}
    <a href="{{ path('admin_rentjeeves_data_contract_list', { "user_id": object.id }) }}"
       class="btn contract_link btn-small"
       title="{{ 'admin.action.contracts'|trans({}) }}">
        <i class="icon-contract"></i>
        {{ 'admin.action.contracts'|trans({}) }}
    </a>
{% endif %}
