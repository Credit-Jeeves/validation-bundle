{% form_theme paymentAccountType 'RjCoreBundle::form.html.twig' %}
{% form_theme userAddressType 'RjCoreBundle::form.html.twig' %}

<div class="form-row clearfix">
    <div class="fields-box">
        <small><b>*</b>{{ 'required' | trans  }}</small>
    </div>
</div>

{{ form_widget(paymentAccountType) }}
{{ form_widget(userAddressType) }}
<div class="form-row clearfix type-checkbox" id="rentjeeves_checkoutbundle_paymentaccounttype_save_row">
    <div class="fields-box">
        <label class="checkbox">
            <input type="checkbox">
            <i> </i>
            <span>{{ 'checkout.payment.save' | trans }}</span>
        </label>
        <i title="{{ 'checkout.payment.save.help' | trans }}" class="help after-checkbox pie-el tooltip-link">?</i>
    </div>
</div>
<p class="note">
    <strong>{{ 'checkout.fee.note' | trans({'%FEE%': paymnet_fee}) }}</strong>
    <i title="{{ 'checkout.fee.note.help' | trans }}" class="help after-checkbox pie-el tooltip-link">?</i>
</p>
